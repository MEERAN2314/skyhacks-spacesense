services:
  - type: web
    name: spacesense-test
    env: python
    region: oregon
    buildCommand: |
      pip install --upgrade pip
      pip install fastapi uvicorn
    startCommand: python test_app.py
    healthCheckPath: /health